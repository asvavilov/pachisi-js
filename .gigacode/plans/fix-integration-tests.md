# План исправления интеграционных тестов

> **Статус: ✅ ЗАВЕРШЁН**
>
> Тест 5 реализован и проходит. Тест 6 уже был исправлен ранее.

## Что было сделано

### Тест 5: «Выпал дубль → дополнительный бросок»

- Реализован в `src/stores/__tests__/integration.test.ts`.
- Проверяет: дубль [3,3] → ход на 3 → ход на 3 → состояние WAIT_ROLL (дополнительный бросок).
- Проверяет `diceStore.used` и `diceStore.unused` на каждом шаге.

### Тест 6: «Правило +7»

- Исправлен ранее: проверка `isPlusSevenActive` заменена на проверку `getAvailableSteps()`.

## Итог

План больше не требует действий.
