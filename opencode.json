{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "quasar": {
      "type": "local",
      "command": ["npx", "-y", "--fetch-retries=0", "@quasar/mcp@latest"],
      "enabled": true,
      "timeout": 30000
    }
  }
}
