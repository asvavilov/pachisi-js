# План доработок для релиза «Парчис»

## 1. Игровая логика — недостающие правила

| #    | Задача                                                                                                                                                                                                                                                                                  | Где                                                   | Приоритет | Статус    |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | --------- | --------- |
| 1.4  | **ИИ для ботов**. Реализовано: чистая логика выбора хода в `src/lib/ai.ts` (эвристики финиш/захват/выход/прогресс + оценка угроз), стор-методы `getCandidateMoves`/`buildAiView`/`aiActOnce`, драйвер `composables/useAiDriver.ts` с задержкой 700 мс и подключением в `IndexPage.vue`. | новый `src/lib/ai.ts` + интеграция в `stores/game.ts` | Высокий   | ✅ Готово |
| 1.5  | **Автопропуск хода при отсутствии возможностей**. По правилам (README п.13) при отсутствии ходов ход передаётся автоматически. Реализовано: `advanceTurnAfterMove()` + автопереход в `rollDice()`.                                                                                      | `stores/game.ts`                                      | Средний   | ✅ Готово |
| 1.6  | **Места 2–4 и завершение игры**. `winners` пополняется в порядке финиша, `next()` пропускает победителей; `places`/`isGameOver`/`activePlayers` в player-store; партия завершается при одном оставшемся игроке или когда остались только ИИ.                                            | `stores/game.ts`, `stores/player.ts`                  | Средний   | ✅ Готово |
| 1.11 | **Барьер + дубль (README п.8)**: при дубле и наличии барьера игрок обязан сдвинуть одну из фишек барьера (если это возможно). Реализовано: `getBarrierChips()` / `isBarrierMoveRequired`, фильтрация `getMovableChips()`.                                                               | `stores/game.ts`                                      | Средний   | ✅ Готово |

## 2. Архитектурные нарушения (AGENTS.md vs реальность)

| #   | Задача                                                                                                                                                                                                                           | Детали |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 2.1 | ✅ **`src/lib/` не зависит от Vue**. Убраны `reactive` из `cell.ts` и `toRaw` из `chip.ts`; `Cell.places` — обычный массив, `Chip.go` — метод (реактивность обеспечивают stores: доска обёрнута в `reactive`, игроки — в `ref`). |
| 2.2 | **Переход от OOP-классов к FP**. `Board`, `Cell`, `Chip`, `Player` — классы с мутабельным состоянием. AGENTS.md требует чистые функции и иммутабельность. Масштабный рефакторинг — отложить на post-release.                     |
| 2.3 | **Бизнес-логика в stores**. `game.ts` содержит правила игры (валидация ходов, расчёт бонусов, определение победителя) — это должно быть в `src/lib/`. Stores должны делегировать вычисления.                                     |
| 2.4 | **`chipIdCounter` — глобальное мутабельное состояние** (`chip.ts`). Заменить на генерацию ID через factory или UUID.                                                                                                             |

## 3. Исправление FIXME/TODO в коде

| #   | Файл                             | Проблема                                                                                                                                                                                                      | Статус    |
| --- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 3.1 | `lib/chip.ts`                    | FIXME: проверки возможности перехода должны быть до вызова `go()`. Исправлено: `go()` сначала проверяет место, не меняет позицию при неудаче и возвращает `boolean`; правила хода проверяет `stores/game.ts`. | ✅ Готово |
| 3.3 | `stores/game.ts`                 | FIXME: проверки хода вынесены в `checkMove()`; списание шага — в `consumeStep()`, где при совпадении бонуса и суммы кубиков приоритет отдаётся кубикам (бонус сохраняется на отдельный ход).                  | ✅ Готово |
| 3.4 | `components/board/CellBoard.vue` | FIXME: подсветка финишных ячеек не фильтруется по дорожке игрока. Исправлено (коммит `f48cfce`): смещение `1000 + playerInd * 100 + idx`.                                                                     | ✅ Готово |

## 4. UI/UX

| #   | Задача                                                                                                                                                                                                                                                                                   | Приоритет |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 4.1 | **Стартовый экран** ✅: состав фиксирован — 1 человек (yellow) + 3 ИИ, кнопка «Начать игру» (`StartScreen.vue`). Настраиваемое количество/цвета отклонены — партия всегда на 4 игрока.                                                                                                   | Высокий   |
| 4.2 | **Экран победы/финиша** ✅: места игроков и кнопка «Новая партия» (сброс фишек `playerStore.reset()` + возврат на старт, `FinishScreen.vue`).                                                                                                                                            | Высокий   |
| 4.3 | ✅ **Заменить raw `<button>` на Quasar-компоненты** (`q-btn`, `q-card`). `GamePanel.vue` переведён на `q-btn`/`q-card` (кнопка броска, выбор хода, отмена).                                                                                                                              | Средний   |
| 4.4 | ✅ **Кости как игровой элемент**: `DiceView.vue` — кубики с пипсами, затемнение использованных, сумма; клик по кости = бросок. Редактируемые `<input>` остались только в debug-панели.                                                                                                   | Средний   |
| 4.5 | ✅ **Анимации**: бросок костей (`DiceView` — `rolling`/`dice-roll`), появление/перемещение фишки (`chip-appear`), сбивание (`game.lastCapturedChipId` + `chip-capture`). Определение происходит через изменение клетки; «перелёт» между клетками не делаем.                              | Низкий    |
| 4.6 | ✅ **Адаптивность (телефон)**: доска масштабируется (`min(100%, 600px)` в портрете, `min(92vh, 600px)` в ландшафте), чипы/кубики уменьшены, базы 2×2, номера клеток скрыты; `IndexPage` — колонка в портрете / ряд в ландшафте; debug-панель только в dev. Портрет и ландшафт проверены. | Средний   |
| 4.7 | ✅ **Визуальная индикация**: подсветка базы текущего игрока (`CornerBoard.vue`), барьеров (`CellBoard.vue` + `isCellBlocked`), безопасных ячеек (было) и легенда в `GamePanel.vue`. Захват — в 4.5.                                                                                      | Средний   |
| 4.8 | ✅ **Клик по целевой ячейке** для перемещения: `gameStore.moveSelectedChipToCell()` + обработчик клика в `CellBoard.vue` (в дополнение к кнопкам в панели).                                                                                                                              | Средний   |

## 5. Интернационализация — отложено

> **Решение:** в ближайших релизах интерфейс только на русском. Текст пишем прямо в шаблонах,
> i18n не внедряем. Мультиязычность вынесена в раздел 8 (следующие релизы).

| #   | Задача                                                                  | Статус      |
| --- | ----------------------------------------------------------------------- | ----------- |
| 5.1 | **Добавить русский язык** (`ru-RU`).                                    | 🔜 Отложено |
| 5.2 | **Вынести все строки в i18n-файлы** (`GamePanel.vue`, `IndexPage.vue`). | 🔜 Отложено |

## 6. Тестирование

> **Статус:** Vitest настроен, все юнит- и интеграционные тесты написаны и проходят — **271 тест / 12 файлов**
> (`npm run test`). Детальный план и матрица тестов — в [`plans/testing-plan.md`](plans/testing-plan.md).
>
> Распределение: `src/lib/` — 78, `src/stores/` — 177, интеграционные — 12, `src/utils/` — 4.
> Покрытие: statements 96.12%, branches 90.97%, functions 98.52%, lines 96.9%.
> `.vue`-компоненты пока не покрыты.

| #   | Задача                                                                                                                                                                                                                     | Приоритет | Статус       |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------ |
| 6.1 | **Настроить тест-раннер** (Vitest).                                                                                                                                                                                        | Высокий   | ✅ Выполнено |
| 6.2 | **Юнит-тесты для игровой логики**: валидация ходов, сбивание, блокады, бонусы, определение победителя, выход из базы (`game.test.ts` — 117 тестов).                                                                        | Высокий   | ✅ Выполнено |
| 6.3 | **Тесты для `dice.ts`**: списание кубиков, сумма, дубли, проверка `diffByCount` (18 тестов).                                                                                                                               | Средний   | ✅ Выполнено |
| 6.4 | **Интеграционные тесты** — полные сценарии игры: полный игровой цикл до FINISH, захват + бонус 20, три дубля, блокада, «+7», барьер + дубль, вывод двух фишек при сумме 5, разыгрывание мест 2–4 после первого победителя. | Высокий   | ✅ Выполнено |
| 6.5 | **Тесты ИИ**: `ai.test.ts` — оценка ходов (финиш/захват/выход/прогресс), угрозы, тай-брейк; `aiActOnce` во всех состояниях и полная партия «ИИ против ИИ» до FINISH.                                                       | Средний   | ✅ Выполнено |

## 7. Технические задачи

| #   | Задача                                                                                                                                                                                                                                                                                                                                                                                                                               | Приоритет | Статус    |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------- | --------- |
| 7.1 | ✅ **Версия** `1.0.0` в `package.json` (RuStore-грейд начинается с 1; до релиза не меняем). В Android: `versionCode 1`, `versionName "1.0.0"` — при добавлении платформы (раздел 9).                                                                                                                                                                                                                                                 | Низкий    | ✅ Готово |
| 7.2 | **Убрать дублирующийся функционал** ✅: из `dice.ts` удалены `hasStart`/`hasOut`/`hasAddon`/`isAllUsed`, оставлен канонический `isOut(steps)` + `isEquals`; `canAddonRollDice` сведён к `isEquals`.                                                                                                                                                                                                                                  | Средний   | ✅ Готово |
| 7.3 | **Проверка `npm run type-check` и `npm run lint`** — убедиться, что проходят без ошибок.                                                                                                                                                                                                                                                                                                                                             | Высокий   | ✅ Готово |
| 7.4 | **Актуальность версий зависимостей** ✅: применены обновления в пределах объявленных диапазонов (`npm update`) — Quasar `2.18.5→2.32.2`, `@quasar/app-vite 2.4.0→2.6.2`, Vue `3.5.22→3.5.42`, Pinia `3.0.3→3.0.4`, vue-router `4.6.3→4.6.4`, vue-i18n `11.1.12→11.4.10`, vue-tsc `3.1.3→3.3.11`, ESLint `9.39.0→9.39.5`, prettier `3.6.2→3.9.6` и др. Мажоры отложены (раздел 8.4). Проверено: тесты, `type-check`, `lint`, `build`. | Средний   | ✅ Готово |

---

## Рекомендуемый порядок работ

### Фаза 1 — Играбельность

- [x] 1.5 — автопропуск хода
- [x] 1.6 — места 2–4 и завершение игры
- [x] 1.11 — барьер + дубль
- [x] 1.4 — ИИ для ботов
- [x] 4.1 — стартовый экран
- [x] 4.2 — экран победы/финиша

> Фаза 1 закрыта: игра играбельна по правилам (1 человек + 3 ИИ, полный цикл до финиша).

### Фаза 2 — Качество

- [x] 6.4 — интеграционные тесты (полные сценарии игры)
- [x] 3.1, 3.3, 3.4 — исправление FIXME

### Фаза 3 — Полировка

- [x] 4.3 — Quasar-компоненты в `GamePanel`
- [x] 4.7 — визуальная индикация
- [x] 4.8 — ход кликом по целевой ячейке
- [x] 4.4 — кости как игровой элемент
- [x] 4.5 — анимации (бросок, появление фишки, захват)
- [x] 4.6 — адаптивность под телефон (портрет и ландшафт)
- [x] 7.2 — дедупликация кода
- [x] ~~5.1–5.2 — i18n~~ → перенесено в раздел 8 (следующие релизы)

### Фаза 4 — Релиз

- [x] 2.1 — убрать Vue-зависимости из `src/lib/`
- [x] 7.1 — версия `1.0.0`
- [x] 7.3 — финальная проверка `type-check` и `lint`
- [x] 7.4 — проверка актуальности версий зависимостей (in-range)

## 8. Предложения на следующие релизы

| #   | Идея                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 8.1 | **Мультиязычность (i18n)**: вынести строки из шаблонов, добавить `ru-RU` и другие локали. Сейчас — только русский, текст в шаблонах.                                                                                                                                                                                                                                                                                                                                                                                                                |
| 8.2 | **Настройка состава 2–4 игроков**: экран выбора количества игроков, для каждого — человек/ИИ и цвет. Сейчас жёстко 1 человек + 3 ИИ.                                                                                                                                                                                                                                                                                                                                                                                                                |
| 8.3 | **Уровни сложности ИИ** (лёгкий/обычный/сложный) и, возможно, доля случайных ходов.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 8.4 | ✅ **Мажорные обновления зависимостей** (выполнено): `@quasar/app-vite 2 → 3.8` (Vite 8 / Rolldown), `vue-router 4 → 5`, `pinia 3 → 4` (+ `@vue/devtools-api 8`), `vitest 4 → 5` + `@vitest/coverage-v8 5`, `eslint 9 → 10` + `@eslint/js 10`, `@quasar/extras 1 → 2`, `globals 16 → 17`, `@types/node 20 → 22`, `vite-plugin-checker 0.11 → 0.14`. Миграция app-vite 3: `#q-app/wrappers → #q-app`, `process.env → import.meta.env.QUASAR_*`, старые алиасы через `build.alias`, удалён `src/env.d.ts`. Тесты/type-check/lint/build/dev — зелёные. |
| 8.5 | **TypeScript 7 — отложен**: `typescript@7` — нативный компилятор без JS-API, `vue-tsc` использует JS-API (`typescript/lib/tsc`). Обходной путь — алиас `typescript` на `@typescript/typescript6` (фактически TS6, без выигрыша). Вернуться, когда Volar поддержит нативный компилятор для `.vue`.                                                                                                                                                                                                                                                   |
| 8.6 | **`@intlify/unplugin-vue-i18n 4 → 11`** — не обновляли (сборка на Vite 8 работает). Проверить/упростить при переходе i18n на JSON-локали.                                                                                                                                                                                                                                                                                                                                                                                                           |

> **Заметка (Windows/OneDrive):** проект лежит в папке OneDrive, и `npm install` периодически падает с `EBUSY` — OneDrive держит файлы в `node_modules`. Перед крупными апдейтами ставить OneDrive на паузу; в идеале вынести репозиторий из OneDrive или исключить `node_modules` из синхронизации.

## 9. Мобильный релиз (Android / RuStore)

> Приоритет: собрать Android-приложение и опубликовать в **RuStore**. Обсудить до реализации.

| #   | Задача                                                                                                                        | Статус       |
| --- | ----------------------------------------------------------------------------------------------------------------------------- | ------------ |
| 9.1 | **Capacitor**: `quasar mode add capacitor`, `npm i @capacitor/core @capacitor/cli @capacitor/android`, `npx cap add android`. | ⬜ Не начато |
| 9.2 | **Версия Android**: `versionCode 1`, `versionName "1.0.0"` (совпадает с `package.json`).                                      | ⬜ Не начато |
| 9.3 | **Ресурсы**: иконка, splash, название приложения; проверить сборку `quasar build -m capacitor -T android` (APK/AAB).          | ⬜ Не начато |
| 9.4 | **Подпись и RuStore**: ключ подписи, AAB, карточка приложения (скриншоты, описание, политика конфиденциальности), загрузка.   | ⬜ Не начато |
| 9.5 | **Проверка на устройстве/эмуляторе**: портрет и ландшафт, бросок и ход тапом, отсутствие прокрутки доски.                     | ⬜ Не начато |

---

> **Примечание**: пункты 2.2–2.3 (полный переход на FP, перенос бизнес-логики из stores в lib) — масштабный рефакторинг, который рискованно делать перед релизом. Рекомендую отложить на post-release v2.
