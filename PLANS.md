# План доработок для релиза «Парчис»

## 1. Игровая логика — недостающие правила

| #    | Задача                                                                                                                                                                              | Где                                                   | Приоритет |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | --------- |
| 1.4  | **ИИ для ботов**. Игроки 1–3 помечены `ai: true`, но логики ИИ нет — ходы ботов не выполняются автоматически. Нужен модуль принятия решений (выбор фишки, выбор хода).              | новый `src/lib/ai.ts` + интеграция в `stores/game.ts` | Высокий   |
| 1.5  | **Автопропуск хода при отсутствии возможностей**. Сейчас игрок вручную нажимает «Завершить ход». По правилам (README п.13 «Отсутствие хода») ход должен пропускаться автоматически. | `stores/game.ts`                                      | Средний   |
| 1.6  | **Места 2–4 и завершение игры**. Игра заканчивается при первом победителе. По правилам остальные продолжают; если остаются только ИИ — игра завершается.                            | `stores/game.ts`, `stores/player.ts`                  | Средний   |
| 1.11 | **Барьер + дубль (README п.8)**: при наличии барьера и дубле игрок обязан сдвинуть одну из фишек барьера.                                                                           | `stores/game.ts`                                      | Средний   |

## 2. Архитектурные нарушения (AGENTS.md vs реальность)

| #   | Задача                                                                                                                                                                                                       | Детали |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| 2.1 | **`src/lib/` не должен зависеть от Vue**. `cell.ts` импортирует `reactive`, `chip.ts` импортирует `toRaw`. Нарушает принцип «чистая логика без Vue-зависимостей».                                            |
| 2.2 | **Переход от OOP-классов к FP**. `Board`, `Cell`, `Chip`, `Player` — классы с мутабельным состоянием. AGENTS.md требует чистые функции и иммутабельность. Масштабный рефакторинг — отложить на post-release. |
| 2.3 | **Бизнес-логика в stores**. `game.ts` содержит правила игры (валидация ходов, расчёт бонусов, определение победителя) — это должно быть в `src/lib/`. Stores должны делегировать вычисления.                 |
| 2.4 | **`chipIdCounter` — глобальное мутабельное состояние** (`chip.ts`). Заменить на генерацию ID через factory или UUID.                                                                                         |

## 3. Исправление FIXME/TODO в коде

| #   | Файл                             | Строка   | Проблема                                                                                                                                |
| --- | -------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| 3.1 | `lib/chip.ts`                    | 37–38    | FIXME: проверки возможности перехода должны быть до вызова `go()`. Сейчас `moveChip` дублирует валидацию.                               |
| 3.2 | `stores/game.ts`                 | —        | ✅ Решено: FIXME про переходную ячейку больше не найден. Проверка промежуточных ячеек реализована в `findTargetCellVariants()` (п.1.7). |
| 3.3 | `stores/game.ts`                 | ~717–720 | FIXME: `moveChip` не должен делать проверки; бонус может совпадать с суммой кубиков — нужно различать для корректного списания.         |
| 3.4 | `components/board/CellBoard.vue` | 47–48    | FIXME: подсветка финишных ячеек не фильтруется по дорожке конкретного игрока.                                                           |

## 4. UI/UX

| #   | Задача                                                                                                                         | Приоритет |
| --- | ------------------------------------------------------------------------------------------------------------------------------ | --------- |
| 4.1 | **Экран настройки игры**: количество игроков (2–4), выбор ИИ/человек для каждого, выбор цвета.                                 | Высокий   |
| 4.2 | **Экран победы/финиша** с местами игроков.                                                                                     | Высокий   |
| 4.3 | **Заменить raw `<button>`/`<input>` на Quasar-компоненты** (`q-btn`, `q-input`). `GamePanel.vue` использует нативные элементы. | Средний   |
| 4.4 | **Кости как игровой элемент**, а не редактируемые `<input>` (сейчас это debug-режим — можно менять значения вручную).          | Средний   |
| 4.5 | **Анимации**: бросок костей, перемещение фишек, сбивание.                                                                      | Низкий    |
| 4.6 | **Адаптивность**: доска зафиксирована на 600px (`MainBoard.vue:35`). Не работает на мобильных.                                 | Средний   |
| 4.7 | **Визуальная индикация**: подсветка текущего игрока, блокад, безопасных ячеек, захвата фишки.                                  | Средний   |
| 4.8 | **Клик по целевой ячейке** для перемещения (а не только кнопки в панели). Сейчас ход делается через кнопки в `GamePanel`.      | Средний   |

## 5. Интернационализация

| #   | Задача                                                                                                                            | Детали |
| --- | --------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 5.1 | **Добавить русский язык** (`ru-RU`). Сейчас весь текст захардкожен в шаблонах на русском, а `i18n/en-US` содержит только примеры. |
| 5.2 | **Вынести все строки в i18n-файлы**. `GamePanel.vue`, `IndexPage.vue` — везде русские строки в шаблонах.                          |

## 6. Тестирование

> **Статус:** Vitest настроен, юнит-тесты (6.1–6.3) написаны и проходят — **202 теста / 10 файлов**
> (`npm run test`). Детальный план и матрица тестов — в [`plans/testing-plan.md`](plans/testing-plan.md).
>
> Распределение: `src/lib/` — 56, `src/stores/` — 142, `src/utils/` — 4.

| #   | Задача                                                                                                                                                                                                                     | Приоритет | Статус       |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------ |
| 6.1 | **Настроить тест-раннер** (Vitest).                                                                                                                                                                                        | Высокий   | ✅ Выполнено |
| 6.2 | **Юнит-тесты для игровой логики**: валидация ходов, сбивание, блокады, бонусы, определение победителя, выход из базы (`game.test.ts` — 87 тестов).                                                                         | Высокий   | ✅ Выполнено |
| 6.3 | **Тесты для `dice.ts`**: списание кубиков, сумма, дубли, проверка `diffByCount` (23 теста).                                                                                                                                | Средний   | ✅ Выполнено |
| 6.4 | **Интеграционные тесты** — полные сценарии игры: полный игровой цикл до FINISH, захват + бонус 20, три дубля, блокада, «+7», барьер + дубль, вывод двух фишек при сумме 5, разыгрывание мест 2–4 после первого победителя. | Высокий   | ⬜ Не начато |

## 7. Технические задачи

| #   | Задача                                                                                                                          | Приоритет |
| --- | ------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 7.1 | **Обновить `package.json` version** с `0.0.1` до релизной (например `1.0.0`).                                                   | Низкий    |
| 7.2 | **Убрать дублирующийся функционал** (TODO из README) — например, `hasOut`/`isOut`/`hasStart` в `dice.ts` частично пересекаются. | Средний   |
| 7.3 | **Проверка `npm run type-check` и `npm run lint`** — убедиться, что проходят без ошибок.                                        | Высокий   |

---

## Рекомендуемый порядок работ

### Фаза 1 — Играбельность

1.4 — ИИ для ботов
1.5 — автопропуск хода
1.6 — места 2–4 и завершение игры
1.11 — барьер + дубль
4.1, 4.2 — экраны настройки и победы

> Цель: игра полностью играбельна по правилам.

### Фаза 2 — Качество

6.4 — интеграционные тесты (полные сценарии игры) ⬜
3.1, 3.3, 3.4 — исправление FIXME (3.2 решён)

### Фаза 3 — Полировка

4.3–4.8 — UI/UX улучшения
5.1–5.2 — i18n (русский язык)
7.2 — дедупликация кода

### Фаза 4 — Релиз

2.1 — убрать Vue-зависимости из `src/lib/`
7.1 — версия
7.3 — финальная проверка `type-check` и `lint`

---

> **Примечание**: пункты 2.2–2.3 (полный переход на FP, перенос бизнес-логики из stores в lib) — масштабный рефакторинг, который рискованно делать перед релизом. Рекомендую отложить на post-release v2.
